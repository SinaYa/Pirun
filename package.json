{
	"name": "pirun",
	"private": true,
	"version": "0.1.0",
	"description": "Local OpenAI-compatible completions proxy with CladGPT inference routing, configured independently of the parent project.",
	"type": "module",
	"scripts": {
		"start": "node src/server.ts",
		"test": "node --test test/*.test.ts",
		"check": "node --check bin/pirun.ts && node --check src/pirun-args.ts && node --check src/pirun-config.ts && node --check src/pirun-antigravity.ts && node --check src/pirun-providers.ts && node --check src/pirun-provider-net.ts && node --check src/pirun-time.ts && node --check src/pirun-files.ts && node --check src/pirun-process.ts",
		"routing": "node -e \"fetch('http://127.0.0.1:8899/v1/routing?model=' + (process.argv[1] || '')).then(r => r.text()).then(console.log)\"",
		"pirun": "node bin/pirun.ts",
		"speed-test": "node bin/speed-test.ts",
		"setup": "node bin/install.ts"
	},
	"dependencies": {
		"yaml": "2.9.0",
		"command-code": "1.32.1",
		"gpt-tokenizer": "4.0.0"
	},
	"engines": {
		"node": ">=22.18.0"
	},
	"bin": {
		"pirun": "bin/pirun.ts"
	}
}
